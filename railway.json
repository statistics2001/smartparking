{
  "buildCommand": "pip install --upgrade pip && pip install -r requirements.txt --force-reinstall",
  "startCommand": "gunicorn --bind 0.0.0.0:$PORT app:create_app() --timeout 120 --workers 2",
  "installCommand": "python -m pip install --upgrade pip setuptools wheel"
}
